version: '3.8'

services:
  lara:
    image: l8tenever/lara:latest
    ports:
      - "80:8000"
    volumes:
      - ./uploads:/app/uploads
    restart: always
    depends_on:
      - clamav

  clamav:
    image: clamav/clamav:latest
    restart: always
